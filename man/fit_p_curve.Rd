% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fit_p_curve}
\alias{fit_p_curve}
\title{Fit estimates of model pi, mu, sigma to a vector of p values.}
\usage{
fit_p_curve(
  p,
  alpha = 1,
  tails = 2,
  alpha_sig = 0.05,
  start = list(pi = 0.5, mu = 2, sigma = 2),
  lower = c(1e-06, 0, 1e-06),
  upper = c(1 - 1e-06, 15, 10)
)
}
\arguments{
\item{p}{Vector of p values in 0--1}

\item{alpha}{Maximum p value in distribution conditional on p<=alpha (default = 1).}

\item{tails}{1 or 2 to indicate 1- or 2-tailed p values (default = 2).}

\item{alpha_sig}{Significance cutoff for power computation}

\item{start}{List of starting parameter values for optim search
(defaults:  pi = 0.5, mu = 2, sigma = 2).}

\item{lower}{Vector of lower bounds for pi, mu, and sigma
(defaults = c(1e-6, 0, 1e-6))}

\item{upper}{Vector of upper bounds for pi, mu, and sigma
(defaults = c(1 - 1e-6, 15, 10))}
}
\value{
List with estimated parameter values, their standard errors
 and 95% confidence limits, etc.
}
\description{
Fit estimates of model pi, mu, sigma to a vector of p values.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fits_for_matrix}
\alias{fits_for_matrix}
\title{Fit p curve separately for each row of a matrix of p values (for simulation)}
\usage{
fits_for_matrix(
  mat_of_ps,
  alpha = 1,
  tails = 2,
  alpha_sig = 0.05,
  want_optim_hessian = FALSE,
  start_parms = pcm_env$optim_starting_parms,
  sig_cutoff_p = 0.05,
  lower = list(mu = 0, sigma = 1e-06, pi = 1e-06),
  upper = list(mu = 20, sigma = 10, pi = 1 - 1e-06)
)
}
\arguments{
\item{mat_of_ps}{Matrix of p values with n_samples rows and n_per_sample columns}

\item{alpha}{Maximum p value in distribution conditional on p<=alpha (default = 1)}

\item{tails}{1 or 2 to indicate 1- or 2-tailed p values (default = 2)}

\item{alpha_sig}{Significance cutoff used in computing the estimated average
power when H0 is false (default = 0.05)}

\item{want_optim_hessian}{Boolean indicating whether optim() should compute
the hessian (default = TRUE, but set to FALSE for faster bootstrapping where
hessian is not used)}

\item{start_parms}{Either a list of starting parameter values for the optim search,
or else a data frame where each row is a combination of starting parameter values
and the function tries all combinations (defaults to optim_starting_parms).
NEWJEFF OBSOLETE start_parms$pi values of NA are replaced with the proportion of to-be-fitted
p values that are significant (i.e., <= sig_cutoff_p)}

\item{sig_cutoff_p}{Significance cutoff used to determine the proportion of
significant to-be-fitted p values for use in adjusting starting value of pi
(default = 0.05)}

\item{lower}{List of lower bounds for the optim search
(defaults: mu = 0, sigma = 1e-6, pi = 1e-6)}

\item{upper}{List of upper bounds for the optim search
(defaults: mu = 20, sigma = 10, pi = 1 - 1e-6)}
}
\value{
Data frame with rows for samples and columns for parameter estimates
}
\description{
Fit p curve separately for each row of a matrix of p values (for simulation)
}

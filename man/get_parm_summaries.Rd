% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_summaries.R
\name{get_parm_summaries}
\alias{get_parm_summaries}
\title{Compute a long-format data frame with summaries of each combination of
summary function & estimated value.}
\usage{
get_parm_summaries(
  estimates,
  parms_to_summarize = "All",
  summary_fns = list(mean = mean, median = stats::median, sd = stats::sd, iqr =
    EnvStats::iqr, min = min, max = max),
  summary_fn_names = NULL
)
}
\arguments{
\item{estimates}{Data frame of parameter estimates for many datasets
with rows for datasets and cols for parameters}

\item{parms_to_summarize}{String vector with names of parameters to estimate
(default = "All")}

\item{summary_fns}{List of the functions to be computed across the different
estimates of each parameter (default = mean = mean, median = median,
sd = sd, iqr, min = min, max = max))}

\item{summary_fn_names}{Optional list of names for the summary function
values (default = NULL, in which case string function name is used)}
}
\value{
long-format data frame with cols for parameter, summary type & value
}
\description{
Compute a long-format data frame with summaries of each combination of
summary function & estimated value.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{nll}
\alias{nll}
\title{Compute negative log-likelihood of a p value or vector of p values
 under the model with the indicated parameters. Use the direct
 method with pdf() or use a censoring method that bins all p's
 less than small_p_bin_cutoff}
\usage{
nll(
  p,
  mu,
  sigma,
  pi = 1,
  alpha = 1,
  tails = 2,
  small_p_bin_cutoff = pcm_env$small_p_bin_cutoff
)
}
\arguments{
\item{p}{Real 0-1 p value or vector for which nll is to be computed. p values
should be checked to make sure they are in range (0--1] before calling
this function.}

\item{mu}{Mean of noncentrality parameters when H1 is true}

\item{sigma}{Standard deviation of noncentrality parameters when H1 is true}

\item{pi}{Proportion of studies with H1 true in mixture of studies with H0 vs H1 true (default = 1)}

\item{alpha}{Maximum p value in distribution conditional on p<=alpha (default = 1)}

\item{tails}{1 or 2 to indicate 1- or 2-tailed p values (default = 2)}
}
\value{
Real negative log-likelihood of the p values
}
\description{
Compute negative log-likelihood of a p value or vector of p values
 under the model with the indicated parameters. Use the direct
 method with pdf() or use a censoring method that bins all p's
 less than small_p_bin_cutoff
}
